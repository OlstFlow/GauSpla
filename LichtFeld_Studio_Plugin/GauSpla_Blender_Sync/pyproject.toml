[project]
name = "gauspla_blender_sync"
version = "0.1.0"
description = "Per-node Blender sync bridge for LichtFeld Studio"
authors = [{name = "OlstFlow & Codex Bro"}]
dependencies = []

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.5.0"
required_features = []
